ENVIRONMENT=<%= node.chef_environment%>
export DJANGO_SETTINGS_MODULE=backend.settings.$ENVIRONMENT

APPS="<%= node.default['webapp']['source_dir']%>"

VENV="<%= node.default['webapp']['venv']%>"

cd $APPS && source $VENV/bin/activate

